# fastsearch · 迭代循环与待办看板

> 状态：**执行中**｜起始 2026-06-25｜上游：[模块拆分](../specs/00-模块拆分.md)、[产品设计](2026-06-24-产品设计文档.md)、[AI_AGENT_DEV_SPEC](../../AI_AGENT_DEV_SPEC.md)。
>
> 目的：把"按 spec 持续迭代到产品完善"落成**可重复执行的循环 + 单一真源待办看板**。每完成一项回写本表（代码是真源，状态如实记账）。

## 1. 迭代循环（每一轮固定动作）

```
① 选题      → 从 §3 看板取"就绪 / 优先级最高 / 当前环境可完整验证"的一项
② 判复杂度  → 复杂（新后端/跨 crate/触不变量/有不确定性）→ 先写/补 spec；简单 → 直接做
③ 实施      → 源码 + 测试同一改动；守住 §2 不变量；偏离计划回写 spec
④ 收口      → cargo fmt --all --check && clippy -D warnings && test --workspace 三绿
              （改检索行为/契约：再实跑 CLI/server 验证一遍）
⑤ 记账      → 回写对应 spec 的"状态/已知限制"，更新本看板该项状态
⑥ 提交      → 一项一 commit（main 本地提交；push 需用户显式要求）
⑦ 回到 ①
```

**选题优先级**（高→低）：(a) 解锁后续的基础件 > (b) 用户可见价值 > (c) 质量护栏。同级别里**当前环境可完整验证**的优先；需外部资源（PG/模型/K8s）的项只能写代码 + 标 `待运行验证`，不算"done"，排到 §3.B。

## 2. 每轮都要守的不变量（摘自 CLAUDE.md，违反即回退）

1. 托管 PG 可移植：PG 侧只用 pgvector + 逻辑复制，绝不要求原生扩展。
2. PG 是真源，引擎索引派生可重建。
3. ACL 不可绕过：新检索入口必须服务端注入 `engine.search(req, Some(acl))`，带越权用例。
4. 确定性：同分按 `GlobalId` 升序 tie-break。
5. 预过滤：SUPERSET 后端查询 + `core::Filter::eval`/`AclFilter::visible` 精确后过滤（两端同步）。
6. 重依赖 opt-in、诚实记账：未真机验证标 `待运行验证`，未落地标 `下一迭代`，别把基线说成生产级。

## 3. 待办看板（单一真源；勾选=收口通过并回写 spec）

状态图例：`[ ]` 未开始 · `[~]` 进行中 · `[x]` 完成（收口三绿+回写）· `[gated]` 代码完成待外部资源验证。

### A. 纯 Rust 可在本环境完整完成（按建议执行序）

| 序 | 项 | crate | F/spec | 状态 |
|---|---|---|---|---|
| A1 | CLI `fastsearch eval <golden.json>` 子命令（暴露 eval 闭环，打印指标/对 baseline 门禁） | cli | F39 | [x] |
| A2 | Filter 算子补全：`Not`/`Ne`/`Exists`（core 已实现求值；text 侧 Ne/Not→精确补集翻译；vector 暴力后过滤已精确） | core,text,vector,engine | F17 | [x] |
| A3 | engine auto-merging（同 section 命中归并，可解释、确定性） | engine | F16 | [x] |
| A4 | server Prometheus 指标完善（请求计数/延迟直方图/各路命中/错误码） | server | F50 | [x] |
| A5 | server 限流 + admission control（令牌桶中间件，超限 429） | server | F50 | [x] |
| A6 | server 审计日志（who/when/query/命中数，结构化、可关） | server | F45 | [x] |
| A7 | text 短语/邻近/前缀查询（phrase+slop 已支持；新增末词 prefix） | text | F8,F9,F35 | [x] |
| A8 | engine **分组折叠**（`Collapse{field,max_per_group}`，按 doc_id/section 限 N 条） | core,engine | F37 | [x] |
| A8b | engine 深分页 `search_after` 游标（与 fusion/rerank 排序、候选窗口纠缠，单列） | engine | F37 | [ ] |
| A9 | vector HNSW（hnsw_rs）+ int8/RaBitQ 量化 + 全精度重排（VectorBackend 新实现） | vector | F13 | [ ] |
| A10 | clients LangChain / LlamaIndex 检索器适配（Python 薄封装 search→Document） | clients/python | F55 | [ ] |
| A11 | text k1/b 自定义 BM25 Scorer（使 TextIndexConfig.k1/b 真生效）。**Tantivy 0.26 硬编码 BM25 参数、未暴露配置**→需自定义 `Scorer` 重算（非快速项，下一迭代）。已先做诚实修正：字段标注"尚未生效" | text | F6 | [~] |
| A12 | engine more_like_this（用命中 chunk 文本反查相似）+ REST /v1/similar | engine,server | F35 | [x] |
| A13 | server OpenAPI/JSON Schema 契约导出（/openapi.json） | server | F54 | [x] |
| A14 | engine 单集合重建 API（坏索引→从真源重灌；换分词器走新 cfg+bootstrap） | engine,text,vector | F33,F48 | [x] |
| A15 | eval golden 集扩充（中文 10 查询/10 corpus + 英文 7 查询/7 corpus 双集，各自 baseline+门禁） | eval,engine | F39 | [x] |

### B. 需外部资源——写代码 + 测试 + 标 `待运行验证`（不算 done）

| 序 | 项 | crate | 资源 | 状态 |
|---|---|---|---|---|
| B1 | sync pgoutput 解码 + CDC 消费（pull_changes）+ 端到端闭环（Docker PG 验证） | sync,engine | ✅ Docker 验证 done | [x] |
| B2 | pg 真 PG 集成验证（含修复 jsonb 写入 bug）；连接池/并发写后续 | pg | ✅ Docker 验证 done（连接池待续） | [x] |
| B3 | sync 初始快照（从 PG 全表 fetch → 灌索引）+ 无缝切换 | sync,engine,server | ✅ Docker 验证 done | [x] |
| B4 | embed 可配置 HTTP 后端（Ollama / OpenAI 兼容），真语义嵌入 | embed | ✅ Ollama 实网验证 done | [x] |
| B4b | embed 接入管线：**server** ingest/query 自动嵌入（Ollama 验证 done）；CLI 待续（向量未落盘，单进程内可用） | server,(cli) | ✅ server 真语义验证 done | [x] |

> 决策（2026-06-25）：**不做进程内模型推理（Candle/ort 编译内置）**。真语义嵌入/rerank 统一经**可配置 HTTP 后端**（Ollama / OpenAI 兼容）接入——绕开重依赖与模型下载，且用户可自由选模型/服务。
| ~~B5~~ | ~~rerank 神经 cross-encoder~~ → **已决策不做**（rerank ADR：RAG 主路径靠答案层 LLM；无-LLM 入口用纯 Rust 轻量 LTR）。见 [spec 21-rerank](../specs/21-rerank.md) | rerank | — | 不做 |
| B6 | vector pgvector 直查档 (a)（ANN 在 PG 跑，后过滤 iterative scan） | vector,pg | 需PG | [ ] |
| B7 | server 容器镜像 + Helm + CloudNativePG 部署样例 | server | 需Docker/K8s | [ ] |

### C. 文档/治理

| 序 | 项 | 状态 |
|---|---|---|
| C1 | lindera/jieba 中文字典许可（CC-BY-SA）分发前审 | [ ] |
| C2 | 容量规划 / SLO-HA 文档（需基准数据） | [ ] |

## 4. "产品完善"出口判据（GA）

- A 组全部 `[x]`；B 组全部 `[gated]`（代码+测试就绪，文档标明验证所需资源与步骤）。
- 收口三绿在 CI 常态化；eval 相关性门禁纳入 CI。
- 四张脸（CLI/库/REST/MCP）契约一致、ACL 不可绕过、引用端到端可解析。
- 每模块 spec 状态为"已完成 vN"或明确 `下一迭代`，无"说成已完成实则没有"的漂移。

## 5. 迭代记录（每轮收口后追加一行）

| 轮次 | 日期 | 项 | 结果 |
|---|---|---|---|
| 0 | 2026-06-25 | eval F39 闭环（golden 加载 + engine::golden::run + 回归门禁） | 已完成（88 测试绿） |
| A1 | 2026-06-25 | CLI `fastsearch eval` 子命令 + 回归门禁退出码 | 已完成（实跑 gate OK，89 测试绿） |
| A2 | 2026-06-25 | text 侧 Ne/Not→索引侧精确补集翻译（vector 已精确，core 已实现） | 已完成（90 测试绿） |
| A3 | 2026-06-25 | engine auto-merging（section 归并，merged_chunk_ids）+ server 透出 | 已完成（92 测试绿） |
| A4 | 2026-06-25 | server Prometheus 指标完善（计数 + 检索延迟直方图） | 已完成（93 测试绿） |
| A5+A6 | 2026-06-25 | server 限流（令牌桶 429）+ 审计日志（可插拔 sink） | 已完成（95 测试绿，活服务验证） |
| A7 | 2026-06-25 | text 短语/邻近（已支持，加测）+ 末词前缀查询（RegexQuery） | 已完成（96 测试绿） |
| A8 | 2026-06-25 | engine 分组折叠 Collapse（每 doc/section 限 N 条）；search_after 拆出 A8b | 已完成（97 测试绿） |
| A13 | 2026-06-25 | server OpenAPI 3.0 契约导出 /openapi.json | 已完成（98 测试绿） |
| A12 | 2026-06-25 | engine more_like_this + REST /v1/similar（ACL 强制、排除种子） | 已完成（100 测试绿） |
| B1a | 2026-06-25 | sync pgoutput 二进制解码（纯函数，对构造字节单测）；连接层仍 gated | 解码已完成（105 测试绿） |
| B2 | 2026-06-25 | pg 真 PG 验证 + 修复 jsonb 写入 bug（$7::text::jsonb） | Docker 验证 done |
| B1 | 2026-06-25 | CDC 闭环：pull_changes（逻辑解码 SQL）+ engine 端到端测试 | Docker 验证 done（PG写→CDC→引擎→检索） |
| B4 | 2026-06-25 | 可配置 HTTP 嵌入后端（Ollama/OpenAI 兼容），真语义 | 本机 Ollama 实网验证 done（语义性 cos 0.31>0.18） |
| B4b | 2026-06-25 | server 接入 embedder（ingest/query 自动嵌入），真语义混合端到端 | 本机 Ollama 验证 done（词面不重叠查询→语义命中） |
| ★ | 2026-06-25 | **完整产品主循环**：CDC 落地自动嵌入（engine.set_embedder）→ PG→复制→解码→嵌入→向量检索 | Docker PG + Ollama 双 env 验证 done（cdc_embed_hybrid_full_loop） |
| 持久化 | 2026-06-25 | 派生索引落盘 + 崩溃安全 CDC 检查点（peek→persist→advance、consume_once） | Docker PG 验证 done（cdc_consume_persist_crashsafe）；124 测试绿 |
| CDC服务 | 2026-06-25 | server 后台 CDC 同步循环（spawn_cdc）+ 落盘恢复（Engine::open） | 活服务验证 done（写PG→cdc applied→语义检索；重启 checkpoint 续传不重嵌） |
| B3 | 2026-06-25 | 初始快照 bootstrap（接入已有 PG 表）+ 无缝衔接；修正 peek LSN 水位误跳 | Docker 验证 done + 活服务（库存量→启动 imported 2→立即可检索）；125 测试绿 |
| 多模态M0 | 2026-06-26 | MM1/3/4/5/6/7 + MM2a/2b：模型/过滤/分面/媒资透出/ACL 网关/golden 门禁/PG 持久化/image_meta→media 彻底迁移 | 本环境全绿 + Docker（pg 往返+CDC 携带 media）；媒资贯通主循环、零字段债 |
| A14 | 2026-06-26 | engine 单集合重建 API（rebuild_from：清空派生→从真源重灌→commit）+ text/vector clear | 本环境 done（rebuild_from_truth_replaces_index）；140 测试绿 |
| A11(诚实) | 2026-06-26 | 标注 BM25 k1/b 尚未生效（Tantivy 0.26 硬编码，需自定义 Scorer）；守诚实记账 | 文档修正 done；真生效列下一迭代 |
| A15 | 2026-06-26 | golden 扩充：中文 10 查询 + 英文 7 查询双集，各自 baseline + 回归门禁（zh nDCG 0.72→0.89） | 本环境 done（3 门禁全绿）；141 测试绿 |
