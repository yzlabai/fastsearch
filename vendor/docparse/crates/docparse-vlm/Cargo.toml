[package]
name = "docparse-vlm"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "VLM annotation + region-recognition backends over OpenAI-compatible services (vLLM/SGLang/Ollama/LM Studio)"

[dependencies]
docparse-core = { path = "../docparse-core" }
docparse-raster = { path = "../docparse-raster" }
anyhow.workspace = true
serde_json.workspace = true
ureq.workspace = true
base64.workspace = true
# Bounded concurrency for batched region reads: the calls are network-bound,
# so a dedicated small pool keeps a served model busy without flooding it.
rayon.workspace = true
